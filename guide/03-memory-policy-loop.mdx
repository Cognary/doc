# Memory and Policy Loop

This page is kept as a compatibility page for older guide links.

## Current path

```text
/how/quickstart
/how/complete-flow
/how/memory-context
/how/policy
```

The maintained loop is still the same runtime shape: write durable memory, recover bounded context, constrain action, persist decision linkage, and feed back the outcome.

```text
write -> planning/context -> tools/select -> tools/decision -> tools/run -> tools/feedback
```
