# SDK Guide

This page is kept as a compatibility page for older guide links.

## Current path

```text
/how/sdk-and-http
/how/codex
/how/mcp
/how/langgraph
```

Use the maintained SDK path when you want current TS SDK, Python SDK, MCP, Codex, and LangGraph examples instead of the older summary page.

```bash
npx @aionis/sdk dev --port 3321
```
