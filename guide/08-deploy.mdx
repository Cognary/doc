# Deploy

This page is kept as a compatibility page for older guide links.

## Current path

```text
/reference/production-ops
/operations
/operations/go-live
/reference/configuration
```

Use the maintained deployment path when you need the current baseline, rollout checks, and rollback ownership.

```bash
npm run start:lite
```
