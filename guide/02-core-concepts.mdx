# Core Concepts

This page is kept as a compatibility page for older guide links.

## Current path

```text
/what
/what/memory-context
/what/handoff
/what/replay
/what/policy
```

The maintained concept set is now split across the capability pages above. That path covers durable memory, bounded context assembly, handoff recovery, replayable execution, and governed tool selection as one connected product model.
