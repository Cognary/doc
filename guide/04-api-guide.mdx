---
title: "API Guide"
description: "Compatibility landing page for older API guide links. Use the current API Reference and endpoint pages instead."
---

# API Guide

This page exists for older guide links.

## Current path

```text
/api-reference
/api-reference/authentication
/api-reference/errors
/reference/api-contract
/reference/common-errors
```

For first integration, start from the endpoint pages instead of this older summary. The smallest maintained path is `write -> recall_text -> rules/evaluate`.
