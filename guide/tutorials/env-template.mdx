# Tutorial: One-click Environment Template

This page is kept as a compatibility page for older tutorial links.

## Current path

```text
/how/quickstart
/how/complete-flow
```

If you specifically want one reusable shell environment for older command flows, keep using the same baseline variables:

```bash
export BASE_URL=http://localhost:3001
export AIONIS_API_KEY=local-dev
export AIONIS_TENANT_ID=default
export AIONIS_SCOPE=docs_v4_env
```
